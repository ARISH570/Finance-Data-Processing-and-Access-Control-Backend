import jwt from 'jsonwebtoken';
import { findUserByEmail, createUser } from '../services/userService.js';
const generateToken = (id) => jwt.sign({ id }, process.env.JWT_SECRET || 'default_jwt_secret', { expiresIn: '7d' });
export const signup = async (req, res, next) => { try { const { name, email, password, role } = req.body; if (!name || !email || !password) return next({ status: 400, message: 'Name,email,and password are required' }); const user = await createUser({ name, email, password, role }); const token = generateToken(user._id); res.status(201).json({ user, token }); } catch (e) { next(e); } };
export const login = async (req, res, next) => { try { const { email, password } = req.body; if (!email || !password) return next({ status: 400, message: 'Email and password are required' }); const user = await findUserByEmail(email); if (!user || user.status === 'inactive') return next({ status: 401, message: 'Invalid credentials' }); const match = await user.matchPassword(password); if (!match) return next({ status: 401, message: 'Invalid credentials' }); res.json({ user, token: generateToken(user._id) }); } catch (e) { next(e); } };
